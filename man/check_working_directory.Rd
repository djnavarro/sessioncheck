% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sessioncheck.R
\name{check_working_directory}
\alias{check_working_directory}
\title{Check the working directory}
\usage{
check_working_directory(
  action = "warn",
  required_wd = NULL,
  action_on_pass = "none"
)
}
\arguments{
\item{action}{Behavior to take if the status is not clean. Possible values are
"error", "warn", "message", and "none". The default is \code{action = "warn"}.}

\item{required_wd}{A single character path giving the working directory the
session is expected to be in. If any other directory is currently in use,
an action is triggered. The default is \code{required_wd = NULL}, which means
there is nothing to compare against, so the check always passes.}

\item{action_on_pass}{Behavior to take if the status is clean. Possible values
are "message" and "none". The default is \code{action_on_pass = "none"}.}
}
\value{
Invisibly returns an object of class \code{sessioncheck_status}.
}
\description{
Individual session check function that inspects the current working
directory. Session checkers can produce errors, warnings, or messages if
requested.
}
\details{
This checker compares the current working directory (\code{getwd()}) against
\code{required_wd}. Both paths are passed through \code{normalizePath()} before
comparison, so differences in trailing slashes or relative vs. absolute
form do not trigger a false positive. When \code{required_wd = NULL} (the
default), there is nothing to compare against, so the check always passes;
the current working directory is still reported in the message.
}
\examples{
check_working_directory(action = "message")

# a working directory that does not match the required path: reports both
# the actual and required paths
check_working_directory(action = "message", required_wd = tempdir())

# a working directory matching the required path: `action` only controls
# what happens when a problem *is* found, so use
# `action_on_pass = "message"` to confirm the clean result instead
check_working_directory(
  action = "none",
  required_wd = getwd(),
  action_on_pass = "message"
)
 
}
\seealso{
\code{\link[=check_attached_packages]{check_attached_packages()}},
\code{\link[=check_loaded_namespaces]{check_loaded_namespaces()}},
\code{\link[=check_globalenv_objects]{check_globalenv_objects()}},
\code{\link[=check_attached_environments]{check_attached_environments()}},
\code{\link[=check_sessiontime]{check_sessiontime()}},
\code{\link[=check_required_options]{check_required_options()}},
\code{\link[=check_required_locale]{check_required_locale()}},
\code{\link[=check_required_sysenv]{check_required_sysenv()}}
}
