% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class.R, R/sessionstate-format.R,
%   R/sessionstatediff-format.R
\name{coercion_methods}
\alias{coercion_methods}
\alias{as.data.frame.sessioncheck_status}
\alias{as.data.frame.sessioncheck_sessioncheck}
\alias{as.data.frame.sessioncheck_sessionstate}
\alias{as.data.frame.sessioncheck_sessionstatediff}
\title{Coerce session check object to a data frame}
\usage{
\method{as.data.frame}{sessioncheck_status}(x, row.names = NULL, optional = FALSE, ...)

\method{as.data.frame}{sessioncheck_sessioncheck}(x, row.names = NULL, optional = FALSE, ...)

\method{as.data.frame}{sessioncheck_sessionstate}(x, row.names = NULL, optional = FALSE, which = "packages", ...)

\method{as.data.frame}{sessioncheck_sessionstatediff}(x, row.names = NULL, optional = FALSE, which = "packages", ...)
}
\arguments{
\item{x}{An object of class \code{sessioncheck_status}, \code{sessioncheck_sessioncheck},
\code{sessioncheck_sessionstate}, or \code{sessioncheck_sessionstatediff}}

\item{row.names}{Ignored}

\item{optional}{Ignored}

\item{...}{Ignored}

\item{which}{For \code{sessioncheck_sessionstate} objects, which tabular
component to return: one of \code{"packages"} (the default), \code{"globalenv"},
or \code{"attachments"}. For \code{sessioncheck_sessionstatediff} objects, the same
three section names instead select a long-format diff table (see
Details); the default is likewise \code{"packages"}. Ignored for other
classes.}
}
\value{
A data frame
}
\description{
S3 \code{as.data.frame()} methods for the three classes this package defines,
letting each be dropped into ordinary data frame workflows (filtering,
joining, export) instead of only being inspected via \code{print()}/\code{format()}.
}
\details{
For \code{sessioncheck_status} and \code{sessioncheck_sessioncheck} objects, this
coercion is lossless: every entity and status recorded in \code{x} appears as a
row in the result. That guarantee does not extend to
\code{sessioncheck_sessionstate} objects: \code{\link[=sessionstate]{sessionstate()}} captures more than
any single rectangular table can hold, mixing scalar fields (\code{platform},
\code{locale}, \code{matrix}, \code{document}, \code{machine}, \code{git}, \code{timing}, \code{rng}), a bare
character vector (\code{libpaths}), and three differently-shaped tables
(\code{packages}, \code{globalenv}, \code{attachments}). \code{as.data.frame()} returns
whichever one of those three tables \code{which} selects; none of the scalar
fields or \code{libpaths} are represented in the result. Use \code{x$platform},
\code{x$machine}, \code{x$git}, \code{x$libpaths}, etc. (or \code{unclass(x)} for everything
at once) to access those directly.

\code{sessioncheck_sessionstatediff} objects (from \code{\link[=compare_sessionstates]{compare_sessionstates()}})
coerce differently again: each \code{which} selects a single long-format
table with one row per key (\code{package}/\code{name}/\code{name}, for
\code{"packages"}/\code{"globalenv"}/\code{"attachments"} respectively) and tracked
field, with columns \verb{<key>}, \code{change} (\code{"added"}, \code{"removed"}, or
\code{"modified"}), \code{field}, \code{old}, and \code{new}. A key present in only one
snapshot contributes one row per tracked field, all with the same
\code{change}, and \code{old}/\code{new} \code{NA} on whichever side it didn't exist; a key
present in both snapshots contributes a row only for fields that
actually changed. The tracked fields are
\code{attached}/\code{ondisk_version}/\code{loaded_version}/\code{source} for \code{"packages"},
\code{class}/\code{size}/\code{hash} for \code{"globalenv"}, and \code{type} for \code{"attachments"}
-- the same fields \code{\link[=compare_sessionstates]{compare_sessionstates()}} tracks for its own
\code{modified} tables. \code{"globalenv"} additionally has a \code{verified} column
(\code{NA} for \code{"added"}/\code{"removed"} rows, since there is nothing to verify
when a key only exists in one snapshot; \code{TRUE}/\code{FALSE} for \code{"modified"}
rows -- see \code{\link[=compare_sessionstates]{compare_sessionstates()}} for what \code{verified} means).
\code{"attachments"} never has \code{"modified"} rows, since a \code{type} change for an
existing search-path entry isn't a realistic scenario.
}
