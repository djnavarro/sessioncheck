% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{check_attachments}
\alias{check_attachments}
\title{Check the search path for non-standard attachments}
\usage{
check_attachments(action = "warn", ignore = NULL)
}
\arguments{
\item{action}{Behaviour to take if package/namespace status is not clean.
Possible values are "error", "warn", "message", and "none".}

\item{ignore}{Character vector containing names of attached environments that
will not trigger an action. The global environment never triggers an action.
If \code{ignore=NULL} the following environment names are ignored: "tools:rstudio",
"tools:positron", "Autoloads"}
}
\value{
Invisibly returns a logical vector with names correspond to the searched
environments as returned by \code{search()}. Values are always set to \code{TRUE} for package
environments and the global environment. For all other environments, the values
are \code{FALSE} unless the environment belongs to the \code{ignore} list.
}
\description{
Check the search path for non-standard attachments
}
\examples{
check_attachment(action = "message")
}
