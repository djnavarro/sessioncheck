% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sessionstatediff.R
\name{compare_sessionstates}
\alias{compare_sessionstates}
\title{Compare two session state snapshots}
\usage{
compare_sessionstates(old, new)
}
\arguments{
\item{old}{A \code{sessioncheck_sessionstate} object (from \code{\link[=sessionstate]{sessionstate()}}),
treated as the baseline.}

\item{new}{A \code{sessioncheck_sessionstate} object (from \code{\link[=sessionstate]{sessionstate()}}),
treated as the later snapshot.}
}
\value{
An object of class \code{sessioncheck_sessionstatediff}, a list with
the same 12 elements as \code{\link[=sessionstate]{sessionstate()}} (\code{platform}, \code{locale}, \code{matrix},
\code{document}, \code{machine}, \code{git}, \code{timing}, \code{rng}, \code{libpaths}, \code{packages},
\code{globalenv}, \code{attachments}), each holding a diff rather than a raw
snapshot. See Details for the shape of each element.
}
\description{
\code{compare_sessionstates()} reports how two \code{\link[=sessionstate]{sessionstate()}} snapshots
differ. This is the comparison counterpart to \code{sessionstate()}'s
point-in-time capture: take a snapshot with \code{sessionstate()}, do some
work, take a second snapshot, and pass both to \code{compare_sessionstates()}
to see what changed.
}
\details{
\code{sessionstate()}'s 12 elements fall into four shapes, and each is
diffed differently:
\itemize{
\item \strong{Record} elements (\code{platform}, \code{locale}, \code{matrix}, \code{document},
\code{machine}, \code{git}, \code{rng}) are named lists of scalar fields. Each is
diffed field-by-field via \code{\link[=identical]{identical()}}, producing a data frame with
columns \code{field}, \code{old}, \code{new}, and \code{changed}.
\item \strong{\code{timing}} is a record element, but \code{captured_at}/\code{elapsed_sec}
necessarily differ between any two calls to \code{sessionstate()}, so
flagging them as "changed" the way other fields are would be noise
every time. Instead \code{timing} reports \code{captured_at_old},
\code{captured_at_new}, \code{wall_elapsed} (the difference between the two
capture times, in seconds), and \code{uptime_elapsed} (the difference
between the two \code{elapsed_sec} values). The two are usually equal; a
mismatch (e.g. the machine slept between snapshots) is itself worth
noticing.
\item \strong{\code{libpaths}} is a plain character vector, diffed via \code{\link[=setdiff]{setdiff()}} in
both directions: \code{list(added = ..., removed = ...)}. Paths present in
both snapshots but reordered are not reported as a change.
\item \strong{Keyed table} elements (\code{packages}, \code{globalenv}, \code{attachments}) are
data frames. Each is diffed into \verb{list(added = <data frame>, removed = <data frame>, modified = <data frame>)} (\code{attachments} has no
\code{modified} table -- a \code{type} change for the same search-path entry
isn't a realistic scenario). \code{added}/\code{removed} are rows present in only
one snapshot (keyed by \code{package}/\code{name}/\code{name} respectively);
\code{modified} covers rows present in both where a tracked column
differs, in a long format with one row per changed field
(\code{package}/\code{name}, \code{field}, \code{old}, \code{new} for \code{packages}; see below for
\code{globalenv}'s slightly different \code{modified} columns).
}

Keyed-table diffing assumes each key (\code{package} for \code{packages}; \code{name}
for \code{globalenv}/\code{attachments}) appears at most once per snapshot -- true
for anything \code{\link[=sessionstate]{sessionstate()}} itself produces. \code{old}/\code{new} are checked
for this on every keyed-table section, and \code{compare_sessionstates()}
errors with an informative message identifying the offending snapshot,
section, and duplicated value(s) if it doesn't hold (e.g. for a
hand-constructed or corrupted \code{sessioncheck_sessionstate} object).

Keyed-table diffing is purely key-based: it has no way to detect a
rename. A package or global environment object that is renamed but
otherwise unchanged between \code{old} and \code{new} (e.g. \code{pkgA} reinstalled
under a new name, or \code{x} renamed to \code{y} via \code{assign()}) is reported as
one \code{removed} row (the old key) plus one \code{added} row (the new key),
never as a single "renamed" entry -- there is no general way to tell a
rename apart from an unrelated removal-plus-addition that happens to
involve similar values. This is inherent to any key-based diff, not a
bug to be fixed.

\code{globalenv}'s \code{modified} table relies on the \code{hash} column
\code{sessionstate()} records for each object (an MD5 fingerprint of the
object's serialized value). When both snapshots have a non-\code{NA} hash for
an object, a hash mismatch is what marks it modified (\code{verified = TRUE});
when either side's hash is \code{NA} (the object couldn't be serialized --
see \code{\link[=sessionstate]{sessionstate()}}'s Global environment section), the comparison falls
back to \code{class}/\code{size} only, and the row is marked \code{verified = FALSE} to
be explicit that a value change could have gone undetected.

A warning is issued if \code{new$timing$captured_at} is earlier than
\code{old$timing$captured_at}, since that usually means the two arguments
were passed in the wrong order; the comparison is still computed either
way.
}
\examples{
baseline <- sessionstate()
# assign() into .GlobalEnv explicitly (rather than `x <- 1:10`) so this
# example is correct wherever it's evaluated: sessionstate() specifically
# inspects .GlobalEnv, but some example/doc runners (e.g. pkgdown) do not
# evaluate example code there
assign("sessioncheck_example_obj", 1:10, envir = .GlobalEnv)
current <- sessionstate()
compare_sessionstates(baseline, current)
rm(sessioncheck_example_obj, envir = .GlobalEnv)

}
\seealso{
\code{\link[=sessionstate]{sessionstate()}}
}
