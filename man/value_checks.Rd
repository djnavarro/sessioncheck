% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{value_checks}
\alias{value_checks}
\alias{check_options}
\alias{check_sysenv}
\alias{check_locale}
\title{Check required values for options, locale, and environment}
\usage{
check_options(action = "warn", required = NULL)

check_sysenv(action = "warn", required = NULL)

check_locale(action = "warn", required = NULL)
}
\arguments{
\item{action}{Behaviour to take if the status is not clean. Possible values are
"error", "warn", "message", and "none". The default is \code{action = "warn"}.}

\item{required}{A named list of required options, locale settings, or environment
variables. If any of these values are missing, or have different values, an
action is triggered.}
}
\value{
Invisibly returns a status flag vector, a logical vector with names
that match those in \code{required}. If the session value matches the required
value, no action is triggered and the status flag is \code{FALSE}. For mismatches
or absent values, the flag is \code{TRUE}.
}
\description{
Individual session check functions that inspect the session options, locale,
or system environment variables. Session checkers can produce errors, warnings,
or messages if requested.
}
\examples{
check_options(action = "message", required = list(scipen = 0L, max.print = 50L))

}
